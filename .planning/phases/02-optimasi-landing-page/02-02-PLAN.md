---
phase: 02-optimasi-landing-page
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - resources/views/orders/show.blade.php
  - resources/views/layouts/app.blade.php
  - resources/views/landing.blade.php
  - public/images/qris.png (placeholder)
autonomous: true
---

<objective>
Optimasi performa landing page untuk target Lighthouse ≥ 80.
</objective>

<tasks>
<task>
<type>execute</type>
<id>02-02-01</id>
<description>Tambah lazy loading dan dimensi eksplisit pada gambar</description>
<files>
  - resources/views/orders/show.blade.php
</files>
<action>
1. Pada QRIS image di `orders/show.blade.php`, tambahkan:
   - `loading="lazy"`
   - `width="300" height="300"`
   - `fetchpriority="high"` (karena QRIS adalah elemen penting di halaman invoice)
</action>
</task>

<task>
<type>execute</type>
<id>02-02-02</id>
<description>Optimasi meta tags dan semantic HTML</description>
<files>
  - resources/views/layouts/app.blade.php
</files>
<action>
1. Di `layouts/app.blade.php`, tambahkan meta description SEO sebelum `</head>`:
```blade
    <meta name="description" content="@yield('meta_description', 'Ultimate ChatGPT Mastery & Prompt Swipe File — Bundle 15.000+ prompt siap pakai untuk marketing, bisnis, dan produktivitas.')">
```
2. Tambahkan `<link rel="preconnect" href="{{ env('UMAMI_HOST_URL') }}"` sebelum tracking script agar DNS lookup lebih cepat saat Umami aktif.
3. Di body, tambahkan `id="app"` ke `<body>` dan `role="application"`.
</action>
</task>

<task>
<type>execute</type>
<id>02-02-03</id>
<description>Production asset build</description>
<files>
  - public/build/ (auto-generated by Vite)
</files>
<action>
1. Jalankan `npm run build` untuk memproduksi CSS/JS production bundle.
2. Hasil build akan masuk ke `public/build/` — sudah di-versioned dan minified oleh Vite.
</action>
</task>

<task>
<type>execute</type>
<id>02-02-04</id>
<description>Hapus unused inline CSS dan pindahkan ke asset</description>
<files>
  - resources/views/landing.blade.php
</files>
<action>
Pindahkan inline style di `@push('styles')` ke file terpisah:
1. Buat `resources/css/landing.css` berisi semua style dari landing.blade.php `@push('styles')` block (kecuali Tailwind utility classes yang sudah di `app.css`).
2. Ganti `@push('styles')` di landing dengan `@vite('resources/css/landing.css')`.
3. Update `vite.config.js` input array untuk include `resources/css/landing.css`.
</action>
</task>
</tasks>

<verification>
1. `npm run build` exits 0
2. File `public/build/` berisi asset landing.css yang ter-versioned
3. Landing page render tanpa inline style block (CSS dari file eksternal)
4. QRIS image memiliki `loading="lazy"` dan atribut dimensi
5. Meta description ada di `<head>`
</verification>
