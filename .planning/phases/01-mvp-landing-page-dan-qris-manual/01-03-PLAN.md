---
phase: 01-mvp-landing-page-dan-qris-manual
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - resources/views/layouts/app.blade.php
  - resources/css/app.css
  - public/images/qris.png
  - storage/app/private/products/Ultimate-ChatGPT-Mastery-Bundle.zip
  - storage/app/private/lead-magnets/50-prompt-pemasaran-gratis.txt
  - tests/Fixtures/proofs/sample-proof.jpg
  - tests/Feature/ScaffoldSmokeTest.php
autonomous: true
requirements: [REQ-001, REQ-002]
must_haves:
  truths:
    - "Base Blade layout renders with viewport meta, CSRF token meta, and title region."
    - "CSS UI tokens match design contract: background #FFF7ED, card #FFFFFF, accent #F97316, destructive #DC2626, controls at least 44px high, system UI font stack."
    - "Private paid ZIP and lead magnet files exist under storage/app/private (not public/storage)."
    - "Test fixture image exists for upload tests without PHP GD dependency."
  artifacts:
    - path: "resources/views/layouts/app.blade.php"
      provides: "Base Blade layout with UI token CSS, viewport meta, CSRF, title section, flash messages"
      min_lines: 30
    - path: "resources/css/app.css"
      provides: "UI token system: background, card, accent, destructive colors, typography, 44px minimum control height, focus ring, status badge classes, validation error styles"
      min_lines: 80
    - path: "public/images/qris.png"
      provides: "Development QRIS placeholder for local rendering"
    - path: "tests/Fixtures/proofs/sample-proof.jpg"
      provides: "Small valid JPG fixture for UploadPaymentProofRequest tests"
  key_links:
    - from: "resources/views/layouts/app.blade.php"
      to: "resources/css/app.css"
      via: "Vite or direct link"
      pattern: "app.css"
    - from: "storage/app/private/products/Ultimate-ChatGPT-Mastery-Bundle.zip"
      to: "config('products.ebook_zip_path')"
      via: "Private file path consumed by DownloadController"
      pattern: "ebook_zip_path"
    - from: "storage/app/private/lead-magnets/50-prompt-pemasaran-gratis.txt"
      to: "config('products.lead_magnet_path')"
      via: "Private file path consumed by LeadMagnetController"
      pattern: "lead_magnet_path"
---

<objective>
Create private asset fixtures, base UI layout, CSS design tokens, and test fixture files for Phase 01.

Purpose: Provide the shared UI shell (Blade layout + CSS tokens), private downloadable assets (paid ZIP, lead magnet text), development QRIS placeholder, and test fixture image that downstream plans (01-04, 01-05, 01-06) consume for rendering and testing.
Output: Base Blade layout, CSS with UI-SPEC tokens, private product/lead magnet assets, QRIS placeholder, test fixture JPG.
</objective>

<execution_context>
@/home/adityasmukti/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/adityasmukti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mvp-landing-page-dan-qris-manual/01-CONTEXT.md
@.planning/phases/01-mvp-landing-page-dan-qris-manual/01-RESEARCH.md
@.planning/phases/01-mvp-landing-page-dan-qris-manual/01-VALIDATION.md
@.planning/phases/01-mvp-landing-page-dan-qris-manual/01-UI-SPEC.md
@.planning/phases/01-mvp-landing-page-dan-qris-manual/01-PATTERNS.md
@.planning/phases/01-mvp-landing-page-dan-qris-manual/01-01-SUMMARY.md

<interfaces>
Design tokens from UI-SPEC for downstream Blade views:
- Background: #FFF7ED, Card/form: #FFFFFF, Accent: #F97316, Destructive: #DC2626
- Status: pending/proof-submitted amber #D97706, verified green #16A34A, rejected destructive
- Body: system UI font stack, 16px/400/1.5
- Heading: 24px/600/1.2, Display: 40px/600/1.1
- Controls: minimum 44px high
- Focus ring: #F97316 at least 2px
- Layout extends via @yield('content') pattern
- Status badge CSS classes: .badge-pending, .badge-proof-submitted, .badge-verified, .badge-rejected
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create private product ZIP, lead magnet, and QRIS placeholder assets</name>
  <read_first>
    @.planning/phases/01-mvp-landing-page-dan-qris-manual/01-RESEARCH.md
    @.planning/phases/01-mvp-landing-page-dan-qris-manual/01-PATTERNS.md
    @docs/Over 15000 ChatGPT Prompts v20.md
    @docs/Over 2499 ChatGPT Prompts Bonus.md
    @docs/4600 Ultimate ChatGPT Prompts Update.md
  </read_first>
  <files>storage/app/private/products/Ultimate-ChatGPT-Mastery-Bundle.zip, storage/app/private/lead-magnets/50-prompt-pemasaran-gratis.txt, public/images/qris.png</files>
  <action>Create private product ZIP at storage/app/private/products/Ultimate-ChatGPT-Mastery-Bundle.zip by bundling the existing docs content files using the `zip` CLI. Create lead magnet text file at storage/app/private/lead-magnets/50-prompt-pemasaran-gratis.txt with 50 marketing prompt entries (one per line, derived from existing docs content). Create public/images/qris.png as a clearly labeled development QRIS stand-in (a simple 300x300 orange PNG with text "QRIS DEVELOPMENT"). Create the private storage directory structure if it does not exist. Do NOT run php artisan storage:link and do not place any of these files on public disk. The production QRIS replacement requirement is documented in this plan's user_setup frontmatter.</action>
  <verify>
    <automated>test -f storage/app/private/products/Ultimate-ChatGPT-Mastery-Bundle.zip && test -f storage/app/private/lead-magnets/50-prompt-pemasaran-gratis.txt && test -f public/images/qris.png</automated>
  </verify>
  <acceptance_criteria>
    - Product ZIP and lead magnet file exist under storage/app/private paths matching config('products.*').
    - QRIS image path exists for rendering and is clearly marked as development placeholder.
    - No files placed under public/storage or storage/app/public.
  </acceptance_criteria>
  <done>Private product/lead assets and QRIS placeholder exist for downstream rendering and download controllers.</done>
</task>

<task type="auto">
  <name>Task 2: Create base Blade layout and CSS design token system</name>
  <read_first>
    @.planning/phases/01-mvp-landing-page-dan-qris-manual/01-UI-SPEC.md
    @.planning/phases/01-mvp-landing-page-dan-qris-manual/01-PATTERNS.md
  </read_first>
  <files>resources/views/layouts/app.blade.php, resources/css/app.css</files>
  <action>Create the base Blade layout with @yield('content'), viewport meta, CSRF meta tag, title section with default fallback, and a flash message area using @if(session('success')) and @if($errors->any()) patterns. Create the plain CSS file (do NOT require Tailwind, PostCSS, or any build step — use Vite's @vite directive from the scaffold) with exact UI tokens from 01-UI-SPEC: body background #FFF7ED, card/form surface #FFFFFF, accent #F97316, destructive #DC2626, system UI font stack, body 16px/400/1.5, label 14px/600/1.4, heading 24px/600/1.2, display 40px/600/1.1, controls minimum 44px height, focus ring #F97316 at least 2px solid outline, visible validation error styles (.error with #DC2626 border/text), status badge classes (.badge-pending amber, .badge-proof-submitted amber, .badge-verified green #16A34A, .badge-rejected destructive), and responsive helpers. Do not add icon libraries, component frameworks, or npm packages.</action>
  <verify>
    <automated>test -f resources/views/layouts/app.blade.php && test -f resources/css/app.css && grep -q "FFF7ED" resources/css/app.css && grep -q "44px" resources/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    - Layout has viewport meta, CSRF token, title section, content yield, and flash message area.
    - CSS contains all UI-SPEC color tokens, typography, focus ring, min control height, status badges, and validation error styles.
    - No icon packages, Tailwind, or additional npm dependencies added.
  </acceptance_criteria>
  <done>Base Blade layout and CSS design tokens are ready for all downstream plan views.</done>
</task>

<task type="auto">
  <name>Task 3: Create test fixture file and update scaffold smoke test</name>
  <read_first>
    @.planning/phases/01-mvp-landing-page-dan-qris-manual/01-VALIDATION.md
    @.planning/phases/01-mvp-landing-page-dan-qris-manual/01-RESEARCH.md
  </read_first>
  <files>tests/Fixtures/proofs/sample-proof.jpg, tests/Feature/ScaffoldSmokeTest.php</files>
  <action>Create a small valid JPG fixture file at tests/Fixtures/proofs/sample-proof.jpg (use a minimal binary JPG or create via PHP with a simple pixel write — do not require PHP GD). Ensure the file is under 50 KB and is a valid image file. Add test methods to ScaffoldSmokeTest.php (from Plan 01-02) to verify: (1) protected files exist on local private disk, (2) paid/lead assets are NOT stored under public/storage, (3) layout contains viewport meta, CSRF meta, and title region, (4) CSS contains #FFF7ED, #FFFFFF, #F97316, #DC2626, 44px minimum control height, and system UI font stack. Do not run php artisan storage:link.</action>
  <verify>
    <automated>test -f tests/Fixtures/proofs/sample-proof.jpg && php artisan test --filter=ScaffoldSmokeTest::test_private_assets_and_ui_tokens</automated>
  </verify>
  <acceptance_criteria>
    - Sample proof JPG exists and is a valid image for upload tests.
    - ScaffoldSmokeTest verifies private assets are not under public/storage.
    - ScaffoldSmokeTest verifies layout and CSS contain all required UI tokens.
  </acceptance_criteria>
  <done>Test fixtures and smoke test coverage for UI/assets are in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blade layout output → browser | User-provided content may render back into HTML; layout escapes by default via {{ }}. |
| private storage → filesystem | Paid ZIP and lead magnet files must remain outside public web root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | ZIP bundle creation | mitigate | Bundle only from existing docs assets; no executable content. |
| T-01-07 | Information Disclosure | private file paths | mitigate | Store ZIP, lead magnet, and proofs on local private disk only; never run storage:link for these files. |
| T-01-08 | Tampering | fixture upload file | mitigate | Fixture is a valid small JPG for testing; never used in production. |
</threat_model>

<verification>
Run after all tasks: test -f tests/Fixtures/proofs/sample-proof.jpg && php artisan test --filter=ScaffoldSmokeTest
</verification>

<success_criteria>
- Private download/proof asset paths are stored under storage/app/private, not publicly exposed.
- Blade layout and CSS tokens match UI-SPEC exactly.
- QRIS placeholder exists for local development (production replacement noted in user_setup).
- Test fixture image is available for upload tests without PHP GD.
- ScaffoldSmokeTest validates UI tokens and private asset isolation.
</success_criteria>

<output>
Create `.planning/phases/01-mvp-landing-page-dan-qris-manual/01-03-SUMMARY.md` when done.
</output>
