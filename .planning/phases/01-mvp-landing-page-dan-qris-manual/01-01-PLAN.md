---
phase: 01-mvp-landing-page-dan-qris-manual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - composer.lock
  - artisan
  - .env.example
  - config/products.php
autonomous: true
requirements: [REQ-001, REQ-002]
user_setup:
  - item: production_qris_asset
    why: "Actual merchant QRIS image is not present in repository; replace public/images/qris.png before launch."
  - item: admin_seed_credentials
    why: "Set ADMIN_EMAIL and ADMIN_PASSWORD in deployment environment before running AdminUserSeeder; never commit credentials."
must_haves:
  truths:
    - "Laravel app boots from workspace root without deleting .planning/ or docs/."
    - "Product configuration is readable at runtime via config('products.*')."
    - "No .env secrets are created or committed by scaffold."
  artifacts:
    - path: "composer.json"
      provides: "Official Laravel app dependencies approved by research Package Legitimacy Audit"
      contains: "laravel/framework"
    - path: "config/products.php"
      provides: "Product price Rp 99.000, QRIS asset path, promo deadline, and private download paths"
      contains: "99000"
    - path: "artisan"
      provides: "Laravel CLI entry point"
  key_links:
    - from: "config/products.php"
      to: "storage/app/private/products/Ultimate-ChatGPT-Mastery-Bundle.zip"
      via: "ebook_zip_path config key"
      pattern: "ebook_zip_path"
    - from: "config/products.php"
      to: "public/images/qris.png"
      via: "qris_image_path config key"
      pattern: "qris_image_path"
---

<objective>
Scaffold the official Laravel foundation and product configuration for Phase 01.

Purpose: Create the bootable Laravel skeleton and product config that all downstream plans implement against. This plan handles only framework scaffold and config — no application code, no routes, no models.
Output: Bootable Laravel app with product configuration and safe .env.example.
</objective>

<execution_context>
@/home/adityasmukti/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/adityasmukti/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-mvp-landing-page-dan-qris-manual/01-CONTEXT.md
@.planning/phases/01-mvp-landing-page-dan-qris-manual/01-RESEARCH.md
@.planning/phases/01-mvp-landing-page-dan-qris-manual/01-PATTERNS.md

<interfaces>
This plan establishes config/products.php with these exact keys for downstream plans:
- price=99000
- ebook_zip_path=products/Ultimate-ChatGPT-Mastery-Bundle.zip
- lead_magnet_path=lead-magnets/50-prompt-pemasaran-gratis.txt
- qris_image_path=/images/qris.png
- promo_deadline from PROMO_DEADLINE env with safe fallback

Order statuses contract: pending, proof_submitted, verified, rejected.
PaymentProof statuses contract: submitted, accepted, rejected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold official Laravel app without removing planning or docs</name>
  <read_first>
    @.planning/phases/01-mvp-landing-page-dan-qris-manual/01-RESEARCH.md
    @.planning/phases/01-mvp-landing-page-dan-qris-manual/01-PATTERNS.md
    @.planning/PROJECT.md
  </read_first>
  <files>composer.json, composer.lock, artisan, app/, bootstrap/, config/, database/, public/, resources/, routes/, storage/, tests/, .env.example</files>
  <action>Install only the approved Composer Laravel skeleton from the Package Legitimacy Audit: create a temporary laravel/laravel project outside the workspace, copy generated app files into this non-empty workspace, preserve existing .planning/ and docs/ exactly, remove the temporary project, and do not install npm/php packages beyond the Laravel skeleton. Do not edit or commit .env. Keep .env.example safe and add non-secret config names PROMO_DEADLINE, ADMIN_EMAIL, ADMIN_PASSWORD, PRODUCT_PRICE=99000, PRODUCT_EBOOK_ZIP_PATH, PRODUCT_LEAD_MAGNET_PATH, and PRODUCT_QRIS_IMAGE_PATH. Do not create any custom models, controllers, routes, views, or migrations beyond the default Laravel skeleton — those come in downstream plans per the split-scope decision.</action>
  <verify>
    <automated>test -f artisan && test -f composer.json && test -d .planning && test -d docs && php artisan about --only=environment</automated>
  </verify>
  <acceptance_criteria>
    - composer.json and artisan exist at workspace root.
    - .planning/ and docs/ still exist with their original planning/content files.
    - No .env secrets are created or committed by this task.
    - Laravel boots with php artisan about.
  </acceptance_criteria>
  <done>Official Laravel scaffold exists in the workspace root and preserves all existing planning/product content.</done>
</task>

<task type="auto">
  <name>Task 2: Create product configuration contract</name>
  <files>config/products.php</files>
  <action>Create config/products.php returning an array with keys: price (integer 99000 per D-11 and REQ-001), ebook_zip_path (string 'products/Ultimate-ChatGPT-Mastery-Bundle.zip'), lead_magnet_path (string 'lead-magnets/50-prompt-pemasaran-gratis.txt'), qris_image_path (string '/images/qris.png'), promo_deadline (env('PROMO_DEADLINE', '2026-07-31') with safe configurable fallback). Add a comment explaining that this config file is the single source of truth for all product pricing and asset paths consumed by downstream plans (01-02 through 01-06). Do not add payment gateway keys, email service config, or any deferred feature config.</action>
  <verify>
    <automated>php -r "echo config('products.price') . PHP_EOL;" 2>/dev/null || echo "config check deferred to Pla 02 when app boots"</automated>
  </verify>
  <acceptance_criteria>
    - config('products.price') returns 99000 integer.
    - All asset paths reference private storage paths (not public/storage).
    - promo_deadline reads from env with a safe non-expired fallback.
  </acceptance_criteria>
  <done>Product config contract is ready for all downstream plans to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Laravel scaffold → workspace | Non-empty workspace must not lose .planning/ or docs/ during scaffold. |
| config/products.php → env | PROMO_DEADLINE reads from environment with safe fallback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Composer install | mitigate | Install only laravel/laravel and laravel/framework approved in RESEARCH Package Legitimacy Audit; no npm/pip/cargo installs in this plan. |
| T-01-02 | Information Disclosure | .env handling | mitigate | Never create or commit .env secrets; add only non-secret config names to .env.example. |
</threat_model>

<verification>
Run after all tasks: test -f artisan && test -f composer.json && test -d .planning && php artisan about --only=environment
</verification>

<success_criteria>
- Laravel scaffold boots at workspace root preserving .planning/ and docs/.
- config/products.php is readable with price=99000 and all asset paths.
- D-11 (Rp 99.000), D-12 (configurable promo deadline) are implemented as config values.
- No .env secrets exist; only non-secret config names in .env.example.
- REQ-001 and REQ-002 foundation (bootable app + config) is present.
</success_criteria>

<output>
Create `.planning/phases/01-mvp-landing-page-dan-qris-manual/01-01-SUMMARY.md` when done.
</output>
